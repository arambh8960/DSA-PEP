class Solution {
    public int largestAltitude(int[] gain) {
        int[] prefix=new int[gain.length+1];
        prefix[0]=0;
        for(int i=1;i<=gain.length;i++){
            prefix[i]=prefix[i-1]+gain[i-1];
        }
        int mx=Integer.MIN_VALUE;
        for(int i=0;i<prefix.length;i++){
            mx=Math.max(prefix[i],mx);
        }
        return mx;
    }
}